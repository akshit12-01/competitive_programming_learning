#include <bits/stdc++.h>

using namespace std;

#pragma region Shortnening Code
#pragma region typedef
typedef long long ll;
typedef vector<int> vi;
typedef pair<int,int> pi;

#pragma endregion

#pragma region Define(Macros)

#define F first;  //now first can be used by F, vector.F
//It can also take params
#define REP(i,a,b) for (int i=a;i<=b;i++)

#pragma endregion

#pragma endregion



//!Modulus
// (a + b) mod m = (a mod m + b mod m) mod m
// (a − b) mod m = (a mod m − b mod m) mod m
// (a · b) mod m = (a mod m · b mod m) mod m

//!Log
// Another property of logarithms is that the number of digits of an
// integer x in base b is floor(logb(x) + 1). For example, the representation of 123 in base
// 2 is 1111011 and floow(log2(123) + 1) = 7

//! To get and send output to files
// freopen("input.txt","r",stdin);
// freopen("output.txt","w", stdout);

main()
{
    //! For Speed,   Also use \n with it to actually sped up
    ios::sync_with_stdio(0);
    cin.tie(0);


}