#include <bits/stdc++.h>

using namespace std;

main()
{
    string var_name;
    float a,b;
    //! for spaces input
    getline(cin, var_name);

    //! If don't know how much data
    while (cin >> var_name)
    {
        // Do whatever you want
    }

    //!Equating Float Numbers
    if (abs(a-b) < 1e-9) {
        // a and b are equal
    }



}